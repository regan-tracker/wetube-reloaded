mixin video(info)
    div
        h4=info.title
        ul 
            li #{info.ratting}/5
            li #{info.comments} comments.
            li Posted #{info.createdAt}
            li #{info.views} views.